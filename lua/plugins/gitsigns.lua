-- Gitsigns provides git integration inside buffers,
-- adding signs to columns to signifiy changes, as well
-- as showing buffer revision history, etc.

return require("functions.git").plugin("Gitsigns")